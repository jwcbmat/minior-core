name: Deploy to Cloud Run

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: '${{ secrets.GCP_SA_KEY }}'

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ vars.GOOGLE_CLOUD_PROJECT }}
          install_components: 'cloud-build,run'

      - name: Build with Cloud Build
        run: |
          gcloud builds submit --tag gcr.io/${{ vars.GOOGLE_CLOUD_PROJECT }}/${{ vars.SERVICE_NAME }}

      - name: Deploy to Cloud Run
        run: |
          gcloud run deploy ${{ vars.SERVICE_NAME }} \
            --image gcr.io/${{ vars.GOOGLE_CLOUD_PROJECT }}/${{ vars.SERVICE_NAME }} \
            --platform managed \
            --region ${{ vars.REGION }} \
            --allow-unauthenticated \
            --port 3000 \
            --set-env-vars=NODE_ENV=${{ vars.NODE_ENV }},GOOGLE_CLOUD_PROJECT=${{ vars.GOOGLE_CLOUD_PROJECT }}
